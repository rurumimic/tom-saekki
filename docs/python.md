## Env

```bash
# pyenv install --list
pyenv install 3.8.7 # pyenv versions
pyenv virtualenv 3.8.7 venv387 # pyenv virtualenvs
```

```bash
echo 'pyenv activate venv387' > .autoenv
echo 'pyenv deactivate' > .autoenv.leave
```
